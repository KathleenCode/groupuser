<html lang="en">
    <%-include('./partials/head.ejs')%>

<body style="background-color: brown;">
    <%-include('./partials/nav.ejs')%>

    <!--%= console.log({users}); %-->

    <div class="table-responsive container">
      <% if(users.length > 0) { %>
        <table class="table caption-top">
          <caption>List of users</caption>
          <thead>
            <tr>
              <th scope="col" class="fw-bold">#</th>
              <th scope="col" class="fw-bold">User Image</th>
              <th scope="col" class="fw-bold">FirstName</th>
              <th scope="col" class="fw-bold">LastName</th>
              <th scope="col" class="fw-bold" >User Email</th>
              <th scope="col" class="fw-bold">Phone Number</th>
              <th scope="col" class="fw-bold">Action</th>
            </tr>
          </thead>

          <tbody>
            <% users.forEach(user => { %>
              <tr>
                <th scope="row">USER</th>
                <td><img src="<%= user.image %>" class="img-thumbnail w-50 h-25 img-fluid" alt="user images"></td>
                <td><%= user.firstName%></td>
                <td><%= user.lastName %></td>
                <td><%= user.email %></td>
                <td><%= user.phone %></td>
                <td>
                    <button class="btn btn-link">
                        <i class="bi bi-pencil-square text-success"></i>
                    </button>
                    <button class="btn btn-link">
                        <i class="bi bi-trash text-danger"></i>
                    </button>
                </td>
              </tr>
            <% }) %>
          
<!--             
            <tr>
              <th scope="row">3</th>
              <td>Image 3</td>
              <td>Larry</td>
              <td>the Bird</td>
              <td>@twitter</td>
              <td>0345567743</td>
              <td>
                  <button class="btn btn-link">
                      <i class="bi bi-pencil-square text-success"></i>
                  </button>
                  <button class="btn btn-link">
                      <i class="bi bi-trash text-danger"></i>
                  </button>
              </td>
            </tr> -->

          </tbody>
        </table>
      <% } else { %>
        <p class="lead text-info text-center fs-2">Nothing to show</p>
      <% } %>
        
    </div>

    <%-include('./partials/foot.ejs')%>
      
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

</body>
</html>